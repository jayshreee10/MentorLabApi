const userController = {
  signUp: (req, res) => {
    res.send({ message: "POST request to the homepage" });
  },

  signIn: (req, res) => {
    res.send({ message: "POST request to the homepage" });
  },
};

export default userController;
